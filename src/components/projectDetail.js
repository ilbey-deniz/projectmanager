import React from 'react';
import {Text, Image, View, StyleSheet} from 'react-native';

const ProjectDetail = () => {
    return <Text>proje detayı</Text>; 
};

const styles = StyleSheet.create({});
export default ProjectDetail;
